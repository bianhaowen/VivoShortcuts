# vivo快捷指令开发提示词

## 开发环境

我需要你帮我开发一个可以在vivo手机上运行的软件，请根据安卓官方推荐，先部署完整的开发环境。


## 程序开发

### 1.0

请帮我开发一个软件
功能：点击桌面图标，弹出悬浮窗，悬浮窗中包含多个按钮，点击不同按钮执行不同功能，点击悬浮窗外区域关闭软件。
按钮功能：将铃声、通知和闹钟的音量调到10%，以10为差值递增，至100%，共计十个按钮，点击按钮时，若当前模式为震动或静音，则将模式改为响铃。
UI：悬浮窗背景为圆角矩形，呈现毛玻璃效果，每个按钮为圆角正方形，颜色为渐变色，每排自左向右平滑渐变，一排五个，每个音量按钮的图标为一个喇叭加上数字，数字使用无衬线字体，整体表现出圆润、温和质感。

### 1.1

进行以下修改：
1、生成apk包名称为XCShortcuts.apk，软件的中文名称为“鑫澄的快捷指令”，英文名称为“XCShortcuts”；
2、编译打包时在release模式下进行，开启编译器优化；
3、程序当前功能不涉及网络下载、读写用户配置文件等需要写文件或缓存的功能，不要出现安装后在文件系统中乱拉屎的现象；
4、程序中调用的API尽可能使用官方推荐的、新的第一方API，尽量不使用老旧的或第三方API。

### 1.2

进行以下修改：
1、软件图标为两行英文字符，第一行为“XC”，第二行为“shortcuts”，字体使用无衬线字体；
2、点击桌面图标后非操作区域不要黑屏，效果类似于系统的通知，不影响其它区域显示当前内容。

### 1.3

安卓软件可以实现ios悬浮球的效果吗？即点击桌面图标后弹出悬浮界面，后台并不启动程序。（或关闭悬浮界面后一同关闭后台程序
  ，无需手动关闭）

### 1.4

进行以下修改：
1、bug：非悬浮窗区域依然为黑色背景，具体表现为点击桌面软件图标后弹出悬浮窗，然后会频闪一下，之后非悬浮窗区域呈现黑色背景。

### 1.5

进行以下修改：
1、还是存在频闪和黑色背景的问题，具体表现为点击桌面软件图标后，先黑屏闪一下，然后出现悬浮窗，此时无黑色背景，又闪烁一下，出现黑色背景。

### 1.6

进行以下修改：
1、还是存在频闪和黑色背景的问题，具体表现和之前一致，请参考vivo originOS6的开发者文档和安卓官方开发者文档，必要时更换API（vivo提供的API被认为是第一方API）或实现方式，修复bug。

### 1.7

进行以下修改：
1、bug：点击桌面软件图标后，闪烁一次后白屏，然后闪退回桌面。

### 2.0

进行以下修改：
1、不再使用悬浮窗模式，改为正常程序；
2、UI：背景为白灰色（偏白），按钮为圆角矩形，每个按钮微凸，按下后凹陷，每行两个按钮，共五行，自左上至右下所有按钮颜色平缓渐变，每个按钮上一个喇叭图案，喇叭图案右边是音量百分比，按钮下无需文本；
3、程序打开默认使用小窗模式，退出至后台后自动销毁。

### 2.1

进行以下优化：
1、无需单独为按钮区域设置一个背景，全屏背景即可；
2、软件图标改为“XCshortcuts”，其中XC显示在第一行，shortcuts显示在第二行。

### 2.2

进行以下优化：
1、参考项目根目录下的shortcuts.png生成一个图标；
2、软件打开后，主页面不显示“音量快捷设置”，只显示按钮即可；
3、按钮及按钮上的音量图标和文字可放大50%，整体布局宽松，不要拥挤，不要聚集在中央小区域内。

### 2.3

进行以下优化：
1、问题：图标太大无法完全显示，请在使用图片shortcuts.png作为图标时，缩放图片，使得图片只占图标中心80%区域，剩下部分使用与图片中的背景色相同的颜色填充；
2、问题：按钮上的图标和文字太大导致换行，请将按钮上的图标和文字缩小至20sp。

进行以下修改：
1、生成apk包名称为XCshortcuts.apk，软件的中文名称为“快捷按钮”，英文名称为“XCshortcuts”。

### 2.4

我更换了图标，重新编译即可，无需修改代码。（这种描述会直接重新编译，如果图标需要根据原图处理生成新图后才能使用，则会更新失败）

请使用项目根目录下的shortcuts.png作为图标。

### 2.5

使用音量键调节音量时会弹出音量调节框，默认只有铃声音量修改，按下音量调节框上的扩展按钮后音量调节框会显示四种全部音量类型调节。
进行以下修改：
1、请在完成音量修改后弹出音量调节框，展示四种全部音量类型的当前音量。

不修改媒体音量，还原为原来的三种。

### 2.9

在音量按钮下方添加两个开关按钮：
按钮一：打开后设置屏幕刷新率到高档，关闭后设置屏幕刷新率到低档；
按钮二：打开后设置屏幕分辨率到高档，关闭后设置屏幕分辨率到低档。
调用系统api实现，且当在系统设置中改变屏幕刷新率或分辨率后，该按钮随之变化。

点击修改刷新率的按钮后直接闪退，点击修改分辨率的按钮后按钮后改变样式，但是在系统设置中查看没有修改分辨率。

完全没有效果，请参考安卓官方开发者手册和vivo官方开发者手册修复bug，实现功能。

手机是vivo x300 pro，操作系统是vivo的OriginOS6，请根据该信息检查api

系统设置中屏幕分辨率有三个选项，分别是“智能适配”，“超清”和“高清”，其中“超清”是2800\*1260，“高清”分辨率是2400\*1080

系统中修改刷新率与分辨率的路径分别为“设置——显示、亮度与护眼——屏幕刷新率”、“设置——显示、亮度与护眼——屏幕刷新率”，系统是否提供了api供调用
